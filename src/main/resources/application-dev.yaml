spring:
  datasource:
    url: jdbc:h2:mem:notificationhubdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  h2:
    console:
      enabled: true
      path: /h2-console  # http://localhost:8080/h2-console

server:
  port: 8080

jwt:
  secret: "cambia-este-secreto-por-uno-largo-y-aleatorio"
  expiration: 3600